#pragma  once

#include <string>

namespace LevelAPI {
    namespace Frontend {
        namespace Color {
            std::string reset();
            std::string red();
            std::string cyan();
            std::string green();
        }
    }
}